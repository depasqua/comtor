
@author Michael E. Locasto

[Intro]

This file contains some code snippets that may or may not be useful in
the future.

Man I miss emacs.

[Code]

        //FileUtil.toFile(NbEditorUtilities.getFileObject(EditorRegistry.lastFocusedComponent().getDocument())) 
        //FileUtil f = null;
        
        //DataObject [] d = new DataObject[0];
        //String path = "";
        //Mode defaultMode;
        //TopComponent [] t;
        //int i = 0;
        //org.openide.windows.TopComponent.Registry registry = TopComponent.getRegistry();
        
        //Node [] n = registry.getCurrentNodes();
        
        //FileUtil f = new FileUtil();

                //iterate over .java files in project, creating JAREntry objects
        //for each one

        //FileObject f = FileUtil.getConfigRoot();
        //1. ask for the project root directory
        //2. then use the JAR API to jar up the src/ directory
        //3. supply a File handle to that JAR file
        
        
        //DataObject.Registry r = DataObject.getRegistry();
        //d = r.getModified();
        
        //defaultMode = WindowManager.getDefault().findMode("editor");
        //t = defaultMode.getTopComponents();
        //for(i=0;i<d.length;i++)
        //{
            //if this TopComponent has a .java file
            
            //how do we get a DataObject from a t[i] ?
        //    path = d[i].getPrimaryFile().getPath();
            //JOptionPane.showMessageDialog(null, path);
        //    JOptionPane.showMessageDialog(this, path);
        //}
                
        //JOptionPane.showMessageDialog(this, FileUtil.getConfigRoot().getPath());
        //org.netbeans.modules.openide.loaders.DataNodeUtils d;
